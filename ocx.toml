#:schema https://ocx.sh/schemas/project/v1.json

[tools]
cli    = "ocx.sh/ocx/cli:0.6"
mirror = "ocx.sh/ocx/mirror:0.6"
# The oxfmt spec is a `url_index` source whose generator is a Python script,
# so the generator runtime is part of the toolchain. Namespaced coordinate —
# the flat `ocx.sh/uv:0` is retired.
uv     = "ocx.sh/astral-sh/uv:0"
